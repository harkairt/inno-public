info:
  name: addUserToSession
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/api/AIWebAPI/addUserToSession"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sessionId": "{{sessionId}}",
        "userCode": "gelencser.erik@innosmartkft.hu",
        "agentId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
