info:
  name: getFile
  type: http
  seq: 6

http:
  method: GET
  url: http://172.22.4.22:8082/api/user/get-selectable-users?email={{email}}
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: email
      value: "{{email}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
