info:
  name: GetSessionHeadersByUserId
  type: http
  seq: 4

http:
  method: POST
  url: http://172.22.4.22:8082/api/AIWebAPI/GetSessionHeadersByUserId
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userCode": "{{email}}",
        "agents": [],
        "filterText": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
