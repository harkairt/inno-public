info:
  name: GetSessionHeadersByUserId
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/api/AIWebAPI/GetSessionHeadersByUserId"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "userCode": "{{email}}",
        "agents": [],
        "filterText": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
