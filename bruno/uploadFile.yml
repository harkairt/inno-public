info:
  name: uploadFile
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/api/AIWebAPI/uploadFile"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/tamas.harkai/Downloads/dev.png
      - name: agentId
        type: text
        value: "148"
      - name: sessionId
        type: text
        value: b6f737e9-9f67-4731-ae49-71cd7ed032ed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
