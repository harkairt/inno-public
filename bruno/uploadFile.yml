info:
  name: uploadFile
  type: http
  seq: 8

http:
  method: POST
  url: http://172.22.4.22:8082/api/AIWebAPI/uploadFile
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /Users/tamas.harkai/Downloads/dev.png
      - name: agentId
        type: text
        value: "148"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
