info:
  name: removeUserFromSession
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/api/AIWebAPI/removeUserFromSession"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sessionId": "{{sessionId}}",
        "userCode": "DEV_iagent_agent8200@innochat.hu",
        "agentId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
